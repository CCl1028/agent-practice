# 基金投资助手 — 技术设计文档

> 版本: 1.0  
> 更新日期: 2026-04-02  
> 状态: MVP 实现完成

---

## 目录

1. [项目概述](#1-项目概述)
2. [架构设计](#2-架构设计)
3. [核心组件详解](#3-核心组件详解)
4. [数据流设计](#4-数据流设计)
5. [状态管理 (LangGraph State)](#5-状态管理-langgraph-state)
6. [Agent 设计](#6-agent-设计)
7. [工具层设计 (Tools)](#7-工具层设计-tools)
8. [API 设计](#8-api-设计)
9. [推送系统](#9-推送系统)
10. [部署架构](#10-部署架构)
11. [扩展设计](#11-扩展设计)

---

## 1. 项目概述

### 1.1 产品定位

一个基于 **多 Agent 架构** 的基金投资助手 MVP，核心价值是：

> "你的基金，我盯着。有事我叫你，没事别操心。"

### 1.2 核心功能

| 功能模块 | 描述 | 状态 |
|---------|------|------|
| 持仓决策助手 | 每日分析持仓，给出加仓/减仓/观望建议 | ✅ 已实现 |
| 基金买前诊断 | 评估一只基金是否值得买（后期迭代） | 🚧 待开发 |
| 涨跌原因分析 | 解释今天为什么涨/跌 | 🚧 待开发 |
| 多渠道推送 | 微信/企业微信/Bark 推送每日简报 | ✅ 已实现 |

### 1.3 技术栈

```
┌─────────────────────────────────────────────────────────────┐
│                        技术选型                              │
├─────────────┬───────────────────────────────────────────────┤
│ Agent 框架  │ LangGraph (基于 LangChain)                     │
│ 后端框架    │ FastAPI                                        │
│ LLM 模型    │ DeepSeek-chat (可切换 OpenAI GPT)              │
│ 数据源      │ AKShare (实时行情) + Mock 兜底                  │
│ 持仓存储    │ JSON 文件 (MVP 阶段)                           │
│ 推送渠道    │ Server酱 / 企业微信 Webhook / Bark             │
│ 定时任务    │ APScheduler (应用内调度)                        │
│ 部署方式    │ Docker + docker-compose                        │
└─────────────┴───────────────────────────────────────────────┘
```

---

## 2. 架构设计

### 2.1 整体架构图

```
                           ┌────────────────────────────────┐
                           │         用户入口层              │
                           │  ┌──────────┐  ┌────────────┐  │
                           │  │  Web UI  │  │  CLI 命令  │  │
                           │  └────┬─────┘  └─────┬──────┘  │
                           └───────┼──────────────┼─────────┘
                                   │              │
                           ┌───────▼──────────────▼─────────┐
                           │         API 网关层              │
                           │         (FastAPI)               │
                           │  /api/briefing                  │
                           │  /api/portfolio                 │
                           │  /api/estimation                │
                           │  /api/push/*                    │
                           └───────────────┬────────────────┘
                                           │
                           ┌───────────────▼────────────────┐
                           │      LangGraph 工作流层         │
                           │                                 │
                           │  ┌─────────────────────────┐   │
                           │  │      Supervisor         │   │
                           │  │    (条件路由入口)        │   │
                           │  └───────────┬─────────────┘   │
                           │              │                  │
                           │    ┌─────────▼─────────┐        │
                           │    │  Portfolio Agent  │        │
                           │    │   (持仓管家)       │        │
                           │    └─────────┬─────────┘        │
                           │              │                  │
                           │    ┌─────────▼─────────┐        │
                           │    │   Market Agent    │        │
                           │    │  (市场观察员)      │        │
                           │    └─────────┬─────────┘        │
                           │              │                  │
                           │    ┌─────────▼─────────┐        │
                           │    │  Briefing Agent   │        │
                           │    │  (简报撰写员)      │        │
                           │    └───────────────────┘        │
                           └─────────────────────────────────┘
                                           │
                           ┌───────────────▼────────────────┐
                           │          工具层 (Tools)         │
                           │  ┌────────────┐ ┌────────────┐ │
                           │  │market_tools│ │portfolio_  │ │
                           │  │ (行情数据) │ │   tools    │ │
                           │  └────────────┘ └────────────┘ │
                           │  ┌────────────┐ ┌────────────┐ │
                           │  │ ocr_tools  │ │ nlp_input  │ │
                           │  │ (截图识别) │ │(自然语言)  │ │
                           │  └────────────┘ └────────────┘ │
                           │  ┌────────────────────────────┐│
                           │  │        push_tools          ││
                           │  │(Server酱/企微/Bark 推送)   ││
                           │  └────────────────────────────┘│
                           └─────────────────────────────────┘
                                           │
                           ┌───────────────▼────────────────┐
                           │          外部服务层             │
                           │  ┌──────┐ ┌───────┐ ┌───────┐ │
                           │  │AKShare│ │DeepSeek││Server酱│ │
                           │  │(行情) │ │ (LLM)  ││(推送)  │ │
                           │  └──────┘ └───────┘ └───────┘ │
                           └─────────────────────────────────┘
```

### 2.2 分层职责

| 层级 | 职责 | 关键文件 |
|-----|------|---------|
| **入口层** | Web UI、CLI 命令行 | `web/index.html`, `main.py` |
| **API 层** | HTTP 接口、请求路由、定时任务调度 | `server.py` |
| **工作流层** | Agent 编排、状态流转 | `src/graph.py` |
| **Agent 层** | 业务逻辑、决策生成 | `src/agents/*.py` |
| **工具层** | 数据获取、外部调用 | `src/tools/*.py` |
| **存储层** | 持仓数据持久化 | `data/portfolio.json` |

---

## 3. 核心组件详解

### 3.1 项目目录结构

```
agent-practice/
├── main.py                 # CLI 入口（每日简报/截图录入/文本录入）
├── server.py               # FastAPI 服务主文件
├── requirements.txt        # Python 依赖
├── Dockerfile              # 容器化配置
├── docker-compose.yml      # 一键部署配置
├── prd.md                  # 产品需求文档
├── README.md               # 项目说明
│
├── src/                    # 核心源码
│   ├── __init__.py
│   ├── config.py           # 全局配置（API Key、模型等）
│   ├── state.py            # LangGraph State 类型定义
│   ├── graph.py            # LangGraph 图定义（核心工作流）
│   ├── formatter.py        # 输出格式化（三层递进）
│   │
│   ├── agents/             # Agent 实现
│   │   ├── portfolio_agent.py   # 持仓管家
│   │   ├── market_agent.py      # 市场观察员
│   │   └── briefing_agent.py    # 简报撰写员
│   │
│   └── tools/              # 工具函数
│       ├── market_tools.py      # 行情/板块/新闻
│       ├── portfolio_tools.py   # 持仓存取/盈亏计算
│       ├── push_tools.py        # 推送（Server酱/企微/Bark）
│       ├── nlp_input.py         # 自然语言录入解析
│       └── ocr_tools.py         # 截图 OCR + LLM 解析
│
├── data/                   # 数据存储
│   └── portfolio.json      # 持仓数据
│
├── web/                    # 前端静态文件
│   └── index.html          # SPA 单页应用
│
└── docs/                   # 文档
    └── design.md           # 本设计文档
```

---

## 4. 数据流设计

### 4.1 每日简报生成流程

```
┌──────────────┐
│  触发入口    │  1. CLI: python main.py
│              │  2. API: POST /api/briefing
│              │  3. 定时: APScheduler (每日 14:30)
│              │  4. Web: 点击"生成今日简报"按钮
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│                    LangGraph 工作流                          │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ Step 1: Supervisor Router                           │    │
│  │ - 读取 state.trigger                                │    │
│  │ - daily_briefing → full_analysis 路径              │    │
│  └─────────────────────┬───────────────────────────────┘    │
│                        │                                    │
│  ┌─────────────────────▼───────────────────────────────┐    │
│  │ Step 2: Portfolio Agent                             │    │
│  │ - 优先使用前端传入的 holdings (localStorage)        │    │
│  │ - 或从 data/portfolio.json 加载                    │    │
│  │ - 调用 AKShare 刷新最新净值                         │    │
│  │ - 计算盈亏比例、获取近5日趋势                        │    │
│  │ - 获取盘中估值 (交易时段)                           │    │
│  │ → 输出: state.portfolio                            │    │
│  └─────────────────────┬───────────────────────────────┘    │
│                        │                                    │
│  ┌─────────────────────▼───────────────────────────────┐    │
│  │ Step 3: Market Agent                                │    │
│  │ - 获取板块涨跌 (AKShare: 行业板块)                  │    │
│  │ - 判断市场情绪 (偏乐观/偏谨慎/中性震荡)             │    │
│  │ - 获取市场热点新闻 (Mock)                           │    │
│  │ → 输出: state.market                               │    │
│  └─────────────────────┬───────────────────────────────┘    │
│                        │                                    │
│  ┌─────────────────────▼───────────────────────────────┐    │
│  │ Step 4: Briefing Agent                              │    │
│  │ - 规则引擎初筛 (基于盈亏+趋势)                       │    │
│  │ - LLM 润色输出 (如配置了 API Key)                   │    │
│  │ - 生成结构化建议                                    │    │
│  │ → 输出: state.briefing                             │    │
│  └─────────────────────┬───────────────────────────────┘    │
│                        │                                    │
└────────────────────────┼────────────────────────────────────┘
                         │
       ┌─────────────────▼─────────────────┐
       │          输出格式化               │
       │  formatter.py                     │
       │  - 推送通知 (1秒看完)             │
       │  - 简报卡片 (10秒看完)            │
       │  - 完整报告 (按需阅读)            │
       └─────────────────┬─────────────────┘
                         │
       ┌─────────────────▼─────────────────┐
       │          推送分发                 │
       │  - Server酱 → 微信               │
       │  - 企业微信 Webhook               │
       │  - Bark → iPhone                 │
       └───────────────────────────────────┘
```

### 4.2 持仓录入流程

```
             ┌───────────────────────────────────────┐
             │            持仓录入入口                │
             ├───────────────┬───────────────────────┤
             │  截图识别      │    自然语言描述        │
             │  (上传图片)    │  ("我买了2万易方达")   │
             └───────┬───────┴───────────┬───────────┘
                     │                   │
         ┌───────────▼───────────┐   ┌───▼─────────────────┐
         │     OCR 识别          │   │   LLM 提取          │
         │  (PaddleOCR)          │   │  (DeepSeek-chat)    │
         │                       │   │                     │
         │  - 识别截图文字       │   │  - 解析自然语言     │
         │  - 提取基金代码/名称  │   │  - 推断基金代码     │
         │  - 提取金额/收益率    │   │  - 提取金额/时间    │
         └───────────┬───────────┘   └───────────┬─────────┘
                     │                           │
                     └─────────────┬─────────────┘
                                   │
                     ┌─────────────▼─────────────┐
                     │      LLM 结构化解析       │
                     │   (JSON 格式输出)         │
                     │                           │
                     │   [{                      │
                     │     "fund_code": "005827",│
                     │     "fund_name": "易方达", │
                     │     "cost": 20000,        │
                     │     "profit_ratio": -5    │
                     │   }]                      │
                     └─────────────┬─────────────┘
                                   │
                     ┌─────────────▼─────────────┐
                     │       合并去重保存        │
                     │  - 按 fund_code 去重      │
                     │  - 新数据覆盖旧数据       │
                     │  → data/portfolio.json   │
                     └─────────────┬─────────────┘
                                   │
                     ┌─────────────▼─────────────┐
                     │     自动触发首次简报      │
                     │  (立即看到操作建议)       │
                     └───────────────────────────┘
```

---

## 5. 状态管理 (LangGraph State)

### 5.1 核心 State 定义

```python
# src/state.py

class FundHolding(TypedDict):
    """单只基金持仓"""
    fund_code: str          # 基金代码 (6位数字)
    fund_name: str          # 基金名称
    cost: float             # 持仓金额 (元)
    cost_nav: float         # 成本净值
    current_nav: float      # 当前净值
    profit_ratio: float     # 盈亏比例 (%)
    hold_days: int          # 持有天数
    trend_5d: list[float]   # 近5日涨跌幅 (%)

class MarketData(TypedDict):
    """市场数据"""
    sectors: list[SectorData]    # 板块涨跌
    market_sentiment: str        # 市场情绪
    hot_news: list[str]          # 热点新闻

class Briefing(TypedDict):
    """最终简报输出"""
    summary: str                 # 一句话结论 (≤15字)
    details: list[FundAdvice]    # 每只基金的建议
    market_note: str             # 市场简评

class AgentState(TypedDict, total=False):
    """LangGraph 全局 State"""
    trigger: Literal["daily_briefing", "user_query", "new_portfolio"]
    user_query: str
    holdings: list[FundHolding]   # 前端传入 (可选)
    portfolio: list[FundHolding]  # Portfolio Agent 填充
    market: MarketData            # Market Agent 填充
    briefing: Briefing            # Briefing Agent 填充
    error: str
```

### 5.2 State 流转示意

```
Initial State                    After Portfolio Agent
┌─────────────────────┐          ┌─────────────────────────────┐
│ trigger: "daily_    │          │ trigger: "daily_briefing"   │
│          briefing"  │    →     │ portfolio: [                │
│ holdings: [...]     │          │   {fund_code: "005827", ...}│
│                     │          │   {fund_code: "161725", ...}│
└─────────────────────┘          │ ]                           │
                                 └─────────────────────────────┘
                                           │
                                           ▼
After Market Agent               After Briefing Agent (Final)
┌─────────────────────────────┐  ┌─────────────────────────────────┐
│ portfolio: [...]            │  │ portfolio: [...]                │
│ market: {                   │  │ market: {...}                   │
│   sectors: [...],           │  │ briefing: {                     │
│   market_sentiment: "偏乐观",│  │   summary: "有基金可考虑止盈",  │
│   hot_news: [...]           │  │   details: [{                   │
│ }                           │  │     fund_name: "招商白酒",       │
└─────────────────────────────┘  │     action: "减仓",             │
                                 │     reason: "浮盈15%且连涨"     │
                                 │   }],                           │
                                 │   market_note: "市场偏乐观"     │
                                 │ }                               │
                                 └─────────────────────────────────┘
```

---

## 6. Agent 设计

### 6.1 Agent 职责划分

```
┌─────────────────────────────────────────────────────────────────┐
│                       Supervisor (路由层)                        │
│                                                                 │
│  职责: 根据 trigger 类型决定执行路径                             │
│  实现: supervisor_router() 函数                                 │
│                                                                 │
│  路由规则:                                                       │
│  - daily_briefing → full_analysis (Portfolio → Market → Brief)  │
│  - new_portfolio  → portfolio_only (仅 Portfolio)               │
│  - user_query     → full_analysis (后期迭代)                    │
└─────────────────────────────────────────────────────────────────┘
                                │
        ┌───────────────────────┼───────────────────────┐
        ▼                       ▼                       ▼
┌───────────────────┐  ┌───────────────────┐  ┌───────────────────┐
│  Portfolio Agent  │  │   Market Agent    │  │  Briefing Agent   │
│    (持仓管家)      │  │   (市场观察员)    │  │   (简报撰写员)    │
├───────────────────┤  ├───────────────────┤  ├───────────────────┤
│ 输入:             │  │ 输入:             │  │ 输入:             │
│ - state.holdings  │  │ - state.portfolio │  │ - state.portfolio │
│   (前端传入)      │  │                   │  │ - state.market    │
│ - 或 JSON 文件    │  │ 输出:             │  │                   │
│                   │  │ - state.market    │  │ 输出:             │
│ 输出:             │  │   - sectors       │  │ - state.briefing  │
│ - state.portfolio │  │   - sentiment     │  │   - summary       │
│   - current_nav   │  │   - hot_news      │  │   - details       │
│   - profit_ratio  │  │                   │  │   - market_note   │
│   - trend_5d      │  │ 工具:             │  │                   │
│   - est_change    │  │ - get_sector_     │  │ 策略:             │
│                   │  │   performance()   │  │ - 规则引擎初筛    │
│ 工具:             │  │ - get_market_     │  │ - LLM 润色输出    │
│ - load_portfolio()│  │   news()          │  │                   │
│ - compute_metrics │  │                   │  │ 决策规则:         │
│ - get_fund_nav()  │  │ 情绪判断:         │  │ - 浮盈>10%+上涨   │
│ - get_fund_       │  │ - avg > 0.5 → 乐观│  │   → 减仓          │
│   estimation()    │  │ - avg < -0.5→ 谨慎│  │ - 浮亏>10%+企稳   │
│                   │  │ - else → 中性     │  │   → 加仓          │
└───────────────────┘  └───────────────────┘  │ - 其他 → 观望     │
                                              └───────────────────┘
```

### 6.2 决策规则引擎详解

```python
# src/agents/briefing_agent.py

def _rule_engine(fund: FundHolding) -> tuple[str, str]:
    """规则引擎：根据盈亏和趋势给出初步建议"""
    profit = fund.get("profit_ratio", 0)  # 盈亏比例
    trend = fund.get("trend_5d", [])       # 近5日趋势
    trend_sum = sum(trend) if trend else 0
    
    is_rising = trend_sum > 1.0   # 近5日累计涨幅 > 1%
    is_falling = trend_sum < -1.0 # 近5日累计跌幅 > 1%

    # 决策矩阵
    #
    #              │  上涨趋势   │  下跌趋势   │  震荡
    # ─────────────┼─────────────┼─────────────┼─────────
    #  浮盈 > 10%  │  减仓(止盈) │  观望       │  观望
    #  浮亏 > 10%  │  观望       │  观望(等企稳)│  加仓(补仓)
    #  盈亏 ≤ 10%  │  观望       │  观望       │  观望
```

### 6.3 LLM 润色流程

```
                     ┌────────────────────────────────┐
                     │        规则引擎初判            │
                     │  (每只基金的 action + reason)  │
                     └───────────────┬────────────────┘
                                     │
                                     ▼
                     ┌────────────────────────────────┐
                     │       构建 Prompt              │
                     │                                │
                     │  System: 你是专业基金助手...   │
                     │                                │
                     │  Human:                        │
                     │  ## 持仓数据                   │
                     │  - 易方达(005827): 盈亏-5%...  │
                     │    规则初判: 观望              │
                     │                                │
                     │  ## 市场数据                   │
                     │  市场情绪: 偏乐观              │
                     │  - 白酒: +2.1%                 │
                     │  - 半导体: -1.5%               │
                     │                                │
                     │  请按 JSON 格式输出...         │
                     └───────────────┬────────────────┘
                                     │
                     ┌───────────────▼────────────────┐
                     │      LLM 调用                  │
                     │  (DeepSeek-chat / GPT-4o)     │
                     └───────────────┬────────────────┘
                                     │
                     ┌───────────────▼────────────────┐
                     │      解析 JSON 输出            │
                     │                                │
                     │  {                             │
                     │    "summary": "今日整体观望",  │
                     │    "details": [...],           │
                     │    "market_note": "市场偏强"   │
                     │  }                             │
                     └───────────────┬────────────────┘
                                     │
               ┌─────────────────────┴─────────────────────┐
               │                                           │
               ▼                                           ▼
    ┌─────────────────────┐                   ┌─────────────────────┐
    │    LLM 成功         │                   │    LLM 失败         │
    │  使用 LLM 润色结果  │                   │  降级为规则引擎结果 │
    └─────────────────────┘                   │  (无需 API Key)     │
                                              └─────────────────────┘
```

---

## 7. 工具层设计 (Tools)

### 7.1 工具模块概览

```
src/tools/
├── market_tools.py      # 市场数据获取
├── portfolio_tools.py   # 持仓管理
├── push_tools.py        # 消息推送
├── nlp_input.py         # 自然语言解析
└── ocr_tools.py         # 截图识别
```

### 7.2 market_tools.py — 市场数据工具

```python
# 核心函数

def is_trading_hours() -> bool:
    """判断是否在交易时段 (工作日 9:30-15:00)"""

def get_fund_estimation(fund_code: str) -> dict | None:
    """获取基金估值
    - 交易时段: 返回盘中实时估值 (AKShare)
    - 非交易时段: 返回上一交易日收盘涨跌
    
    返回: {
        "est_nav": 2.05,        # 估算净值
        "est_change": -0.85,    # 估算涨跌幅
        "est_time": "15:00",    # 估值时间
        "is_live": True/False   # 是否实时
    }
    """

def get_fund_nav(fund_code: str) -> dict:
    """获取基金最新净值 + 近5日趋势
    - 优先 AKShare 实时数据
    - 失败则 Mock 兜底
    
    返回: {
        "current_nav": 2.03,
        "date": "2026-04-02",
        "trend_5d": [-0.3, 0.5, -0.8, 0.2, -1.1]
    }
    """

def get_sector_performance() -> list[dict]:
    """获取板块涨跌 (Top 10)
    
    返回: [
        {"name": "白酒", "change": 2.1},
        {"name": "半导体", "change": -1.5},
        ...
    ]
    """

def get_market_news() -> list[str]:
    """获取市场热点新闻 (MVP: Mock)"""
```

### 7.3 portfolio_tools.py — 持仓管理工具

```python
# 核心函数

def load_portfolio() -> list[FundHolding]:
    """从 data/portfolio.json 加载持仓
    - 文件不存在时返回 Mock 数据
    """

def save_portfolio(portfolio: list[FundHolding]) -> None:
    """保存持仓到 JSON 文件"""

def compute_metrics(portfolio: list[FundHolding]) -> list[FundHolding]:
    """刷新持仓指标
    - 获取最新净值
    - 计算盈亏比例
    - 获取近5日趋势
    - 获取盘中估值
    """
```

### 7.4 push_tools.py — 推送工具

```python
# 支持的推送渠道

┌─────────────────────────────────────────────────────────────┐
│ 渠道        │ 优点              │ 配置项                     │
├─────────────┼───────────────────┼────────────────────────────┤
│ Bark        │ iOS 原生推送，快  │ BARK_URL                  │
│ Server酱    │ 微信推送，免费    │ SERVERCHAN_KEY            │
│ 企业微信    │ 群内推送，稳定    │ WECOM_WEBHOOK_URL         │
└─────────────────────────────────────────────────────────────┘

# 核心函数

def push_briefing(briefing: dict) -> dict:
    """推送简报到所有已配置渠道
    
    返回: {
        "bark": True/False/None,
        "serverchan": True/False/None,
        "wecom": True/False/None
    }
    """
```

### 7.5 数据源降级策略

```
                          数据请求
                             │
                             ▼
            ┌────────────────────────────────┐
            │       尝试 AKShare API         │
            │  (fund_value_estimation_em)    │
            └───────────────┬────────────────┘
                            │
             ┌──────────────┴──────────────┐
             │                             │
          成功│                          失败│
             ▼                             ▼
    ┌────────────────┐          ┌────────────────┐
    │  返回真实数据  │          │  使用 Mock 数据 │
    │  is_live: True │          │  is_live: False │
    └────────────────┘          └────────────────┘

# Mock 数据设计原则:
# 1. 覆盖常见基金代码 (005827, 161725, 110011)
# 2. 提供合理的默认值，不随机生成
# 3. 确保无 API Key 也能完整体验流程
```

---

## 8. API 设计

### 8.1 API 端点列表

```
┌────────────────────────────────────────────────────────────────────┐
│                           API 端点                                  │
├─────────────────────────┬──────────┬───────────────────────────────┤
│ 端点                     │ 方法     │ 描述                          │
├─────────────────────────┼──────────┼───────────────────────────────┤
│ /api/briefing           │ POST     │ 生成每日简报                   │
│ /api/portfolio          │ GET      │ 获取当前持仓列表               │
│ /api/portfolio/add-text │ POST     │ 自然语言录入持仓               │
│ /api/portfolio/add-screenshot │ POST│ 截图识别录入持仓               │
│ /api/portfolio/parse-text │ POST   │ 解析文本 (只解析不保存)        │
│ /api/portfolio/parse-screenshot │POST│ 解析截图 (只解析不保存)       │
│ /api/portfolio/{code}   │ DELETE   │ 删除指定持仓                   │
│ /api/estimation         │ GET/POST │ 获取持仓盘中估值               │
│ /api/push/status        │ GET      │ 获取推送渠道配置状态           │
│ /api/push/test          │ POST     │ 发送测试推送                   │
│ /api/briefing-and-push  │ POST     │ 生成简报并推送                 │
│ /api/config             │ GET/POST │ 读取/更新配置                  │
│ /api/health             │ GET      │ 健康检查                       │
└─────────────────────────┴──────────┴───────────────────────────────┘
```

### 8.2 核心 API 详解

#### POST /api/briefing — 生成每日简报

```json
// 请求
{
  "holdings": [  // 可选，前端传入的持仓
    {
      "fund_code": "005827",
      "fund_name": "易方达蓝筹精选",
      "cost": 20000,
      "cost_nav": 2.15,
      "profit_ratio": -5
    }
  ]
}

// 响应
{
  "notification": "📊 今日持仓：今天无需操作 ✅",
  "card": "┌───────────────────────────────┐\n│ 易方达蓝筹 👉 观望 ⏸️\n...",
  "report": "======== 📋 今日投资简报 ========\n...",
  "raw": {
    "summary": "今天无需操作",
    "details": [...],
    "market_note": "市场情绪中性震荡"
  }
}
```

#### POST /api/portfolio/add-text — 自然语言录入

```json
// 请求
{
  "text": "我去年6月买了2万易方达蓝筹，现在亏了5个点"
}

// 响应
{
  "added": [
    {
      "fund_code": "005827",
      "fund_name": "易方达蓝筹精选",
      "cost": 20000,
      "profit_ratio": -5,
      "hold_days": 280
    }
  ],
  "total": 3  // 合并后总持仓数
}
```

### 8.3 定时任务设计

```python
# server.py

# APScheduler 配置
scheduler = BackgroundScheduler(timezone="Asia/Shanghai")
DEFAULT_PUSH_TIME = "14:30"  # 默认下午两点半推送

def _scheduled_push():
    """定时任务：生成简报并推送"""
    result = langgraph_app.invoke({"trigger": "daily_briefing"})
    briefing = result.get("briefing", {})
    push_briefing(briefing)

# 启动时根据 .env 中的 PUSH_TIME 配置调度
# 支持动态更新推送时间
```

---

## 9. 推送系统

### 9.1 三层递进输出设计

```
┌─────────────────────────────────────────────────────────────────┐
│                        三层递进输出                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  第一层：推送通知 (1秒看完)                                      │
│  ────────────────────────────────                               │
│  📊 今日持仓：今天无需操作 ✅                                     │
│                                                                 │
│  适用场景: 手机锁屏通知、微信消息预览                             │
│  设计目标: 一眼判断是否需要关注                                   │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  第二层：简报卡片 (10秒看完)                                     │
│  ────────────────────────────────                               │
│  ┌─────────────────────────────────────┐                        │
│  │  易方达蓝筹  👉 观望 ⏸️              │                        │
│  │  招商白酒    👉 减仓 🔴              │                        │
│  │                                     │                        │
│  │  市场：偏乐观                        │                        │
│  │  💡 有基金可考虑止盈                 │                        │
│  └─────────────────────────────────────┘                        │
│                                                                 │
│  适用场景: 点开消息快速浏览                                      │
│  设计目标: 快速了解每只基金的建议                                 │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  第三层：完整报告 (按需阅读)                                     │
│  ────────────────────────────────                               │
│  ======== 📋 今日投资简报 ========                              │
│                                                                 │
│  📌 总结：有基金可考虑止盈                                       │
│                                                                 │
│  📊 持仓建议：                                                   │
│  ────────────────────────                                       │
│                                                                 │
│  🔴 招商白酒                                                    │
│     操作：减仓（置信度：高）                                     │
│     理由：浮盈15.6%且短期连涨，可考虑止盈                         │
│                                                                 │
│  ⏸️ 易方达蓝筹                                                  │
│     操作：观望（置信度：中）                                     │
│     理由：盈亏幅度不大，无明显趋势信号                            │
│                                                                 │
│  🌐 市场简评：市场情绪偏乐观，白酒板块领涨                        │
│                                                                 │
│  适用场景: 想了解详细分析逻辑时查看                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 9.2 推送渠道对比

```
┌─────────────────────────────────────────────────────────────────┐
│                       推送渠道对比                               │
├──────────┬────────────┬────────────┬────────────────────────────┤
│ 渠道      │ 推送目标   │ 消息格式    │ 适用场景                   │
├──────────┼────────────┼────────────┼────────────────────────────┤
│ Bark     │ iPhone     │ 短文本     │ 个人用户，需要即时通知      │
│          │            │            │ 最快最简单                  │
├──────────┼────────────┼────────────┼────────────────────────────┤
│ Server酱 │ 微信       │ Markdown   │ 个人用户，习惯用微信        │
│          │            │            │ 免费额度够用                │
├──────────┼────────────┼────────────┼────────────────────────────┤
│ 企业微信 │ 企微群     │ Markdown   │ 团队共享，多人查看          │
│          │            │            │ 无需额外 App                │
└──────────┴────────────┴────────────┴────────────────────────────┘
```

---

## 10. 部署架构

### 10.1 Docker 部署架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    腾讯云轻量应用服务器                           │
│                    (2核2G, Ubuntu + Docker)                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   ┌─────────────────────────────────────────────────────────┐   │
│   │                Docker Compose                           │   │
│   │                                                         │   │
│   │   ┌─────────────────────────────────────────────────┐   │   │
│   │   │           fund-assistant 容器                    │   │   │
│   │   │                                                 │   │   │
│   │   │   ┌───────────────────────────────────────┐     │   │   │
│   │   │   │         FastAPI (uvicorn)             │     │   │   │
│   │   │   │         监听 0.0.0.0:8000             │     │   │   │
│   │   │   └───────────────────────────────────────┘     │   │   │
│   │   │                     │                           │   │   │
│   │   │   ┌─────────────────▼───────────────────┐       │   │   │
│   │   │   │        APScheduler                  │       │   │   │
│   │   │   │   每日 14:30 触发 _scheduled_push() │       │   │   │
│   │   │   └─────────────────────────────────────┘       │   │   │
│   │   │                                                 │   │   │
│   │   │   挂载卷:                                        │   │   │
│   │   │   - ./data:/app/data (持仓数据)                 │   │   │
│   │   │   - ./.env:/app/.env (环境变量)                 │   │   │
│   │   │                                                 │   │   │
│   │   │   环境变量:                                      │   │   │
│   │   │   - TZ=Asia/Shanghai                            │   │   │
│   │   │                                                 │   │   │
│   │   └─────────────────────────────────────────────────┘   │   │
│   │                                                         │   │
│   └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│   防火墙: 开放 8000 端口                                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

# docker-compose.yml 配置要点:
# - restart: always  # 容器异常自动重启
# - volumes 持久化数据和配置
# - TZ 时区设置确保定时任务准确
```

### 10.2 部署流程

```bash
# 首次部署
rsync -avz ./ root@<IP>:/root/fund-assistant/  # 上传代码
ssh root@<IP>
cd /root/fund-assistant
vim .env                                        # 配置环境变量
docker compose up -d --build                   # 构建并启动

# 更新代码
rsync -avz ./ root@<IP>:/root/fund-assistant/
ssh root@<IP> "cd /root/fund-assistant && docker compose up -d --build"
```

---

## 11. 扩展设计

### 11.1 后续迭代方向

```
┌─────────────────────────────────────────────────────────────────┐
│                       后续迭代 Roadmap                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Phase 2: 基金诊断模块                                          │
│  ──────────────────────                                         │
│  - 新增 Diagnosis Agent                                         │
│  - 输入基金代码 → 输出是否值得买                                 │
│  - 评估维度: 近一年收益、最大回撤、规模、持仓集中度               │
│                                                                 │
│  Phase 3: 涨跌原因分析                                          │
│  ──────────────────────                                         │
│  - 接入财经新闻 API                                              │
│  - 关联持仓行业与新闻                                            │
│  - LLM 生成涨跌归因分析                                          │
│                                                                 │
│  Phase 4: Chat 对话入口                                         │
│  ──────────────────────                                         │
│  - 支持自然语言查询: "我的基金怎么样了?"                         │
│  - 意图识别 → 路由到对应 Agent                                   │
│  - 接入微信/企微作为对话入口                                     │
│                                                                 │
│  Phase 5: 数据存储升级                                          │
│  ──────────────────────                                         │
│  - JSON → SQLite → PostgreSQL                                   │
│  - 支持多用户                                                    │
│  - 历史建议回溯                                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 11.2 Agent 扩展点设计

```python
# src/graph.py 预留的扩展点

def supervisor_router(state: AgentState) -> str:
    trigger = state.get("trigger", "daily_briefing")
    
    if trigger == "new_portfolio":
        return "portfolio_only"
    
    elif trigger == "user_query":
        # TODO: 解析用户意图，分流到不同 Agent
        # - "这只基金值不值得买?" → diagnosis_agent
        # - "今天为什么跌了?" → explanation_agent
        # - "我的基金怎么样?" → full_analysis
        return "full_analysis"
    
    elif trigger == "diagnosis":
        # TODO: Phase 2 实现
        return "diagnosis_agent"
    
    else:
        return "full_analysis"
```

### 11.3 并行执行优化

```
当前实现 (顺序执行):
Portfolio → Market → Briefing

优化方案 (并行执行):
                    ┌── Portfolio ──┐
    Supervisor ─────┤               ├── Briefing
                    └── Market ─────┘

实现方式:
- 使用 LangGraph 的 parallel 或 fan-out/fan-in 模式
- Portfolio 和 Market 无依赖，可并行
- Briefing 等待两者完成后执行
```

---

## 附录

### A. 环境变量配置

```bash
# .env 文件示例

# LLM 配置 (可选，无 Key 也能跑)
OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=https://api.deepseek.com/v1

# 推送配置 (至少配一个)
SERVERCHAN_KEY=SCTxxx          # Server酱 SendKey
WECOM_WEBHOOK_URL=https://...  # 企业微信 Webhook
BARK_URL=https://api.day.app/xxx  # Bark 推送 URL

# 定时推送时间 (可选，默认 14:30)
PUSH_TIME=14:30
```

### B. Mock 数据说明

| 基金代码 | 基金名称 | Mock 净值 | Mock 趋势 |
|---------|---------|----------|----------|
| 005827 | 易方达蓝筹精选 | 2.03 | 震荡下跌 |
| 161725 | 招商中证白酒 | 1.85 | 连续上涨 |
| 110011 | 易方达中小盘 | 4.52 | 弱势震荡 |

### C. 术语表

| 术语 | 解释 |
|-----|------|
| NAV | Net Asset Value，基金净值 |
| 估值 | 盘中根据持仓股票走势预估的净值 |
| 趋势 | 近5个交易日的涨跌幅序列 |
| 规则引擎 | 基于盈亏和趋势的决策逻辑 |
| 降级 | LLM 不可用时使用规则引擎结果 |

---

*文档结束*
